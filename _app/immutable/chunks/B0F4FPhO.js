import"./DsnmJJEf.js";import"./B8phH7r5.js";import{O as u,P as a,Q as g,R as h,S as t,T as o,U as m}from"./m6VW359Z.js";import{h as s}from"./BGy7xXYn.js";const f={title:"Sensors and Actuators",description:"Configuration and usage of sensors and actuators in ThinkEngine, including mappings, triggers, and debugging.",order:3,tags:["ThinkEngine","sensors","actuators","Unity","ASP","configuration"]},{title:S,description:w,order:x,tags:T}=f;var k=u('<h1 id="3-sensors-and-actuators">3. Sensors and Actuators</h1> <p>ThinkEngine connects the game world to the reasoning layer through <strong>Sensors</strong> and <strong>Actuators</strong>.</p> <p>Sensors read selected properties from your GameObjects and convert them into ASP facts.</p> <p>Actuators write back values decided by the solver, modifying the game scene at runtime.</p> <p>This section explains how to add and configure these components correctly.</p> <hr/> <h2 id="31-sensorconfiguration">3.1 SensorConfiguration</h2> <p>A <strong>SensorConfiguration</strong> defines which GameObject properties are exposed to the reasoning layer.</p> <p>At runtime, these properties are read at a fixed update frequency and translated into ASP facts (e.g., positions, states, counters, health values, etc.).</p> <h3 id="adding-a-sensor">Adding a Sensor</h3> <p>1. Select the GameObject you want to observe (e.g., <code>Agent</code> or <code>Target</code>).</p> <p>2. Click <strong>Add Component → Sensor Configuration</strong>.</p> <p>3. Give the configuration a unique name (e.g., <code>agentSensor</code>).</p> <p>4. In the property tree, check the properties to expose (e.g., <code>transform.position.x</code>, <code>transform.position.y</code>, <code>transform.position.z</code>).</p> <p>5. Save the configuration.</p> <p>🧠 <strong>Tip:</strong> When you save the configuration, ThinkEngine automatically adds a <code>MonoBehaviourSensorsManager</code> component to the same GameObject.</p> <p>This manager handles sensor instantiation at runtime.</p> <h3 id="supported-property-types">Supported Property Types</h3> <ul><li><p>Scalar values (<code>float</code>, <code>int</code>, <code>bool</code>, <code>string</code>)</p></li> <li><p>Lists and arrays (1D or 2D)</p></li> <li><p>Basic structs (e.g., <code>Vector3</code> → split into <code>x</code>, <code>y</code>, <code>z</code>)</p></li> <li><p>No support for dictionaries (as of current version)</p></li></ul> <h3 id="aggregation">Aggregation</h3> <p>For properties with historical values, you can choose how the fact is generated:</p> <ul><li><p><code>newest</code> (default)</p></li> <li><p><code>oldest</code></p></li> <li><p><code>avg</code> (average)</p></li> <li><p><code>min</code> / <code>max</code></p></li></ul> <p><strong>Example fact generated at runtime:</strong></p> <pre class="language-prolog"><!></pre> <hr/> <h2 id="311-customizing-sensor-predicate-names">3.1.1 Customizing Sensor Predicate Names</h2> <p>When configuring a SensorConfiguration, each mapped property is automatically translated into an ASP predicate (e.g., <code>agentSensor_x(agent,objectIndex(0),value)</code>).</p> <p>However, you can customize the name of this predicate during sensor configuration:</p> <p>1. In the SensorConfiguration inspector, select the property.</p> <p>2. Click <strong>Configure</strong>.</p> <p>3. Edit the predicate name that will be used in the generated ASP facts.</p> <p><strong>Example:</strong></p> <table><thead><tr><th>Property</th><th>Default fact</th><th>Customized predicate</th></tr></thead><tbody><tr><td><code>transform.position.x</code></td><td><code>agentSensor_x(agent,objectIndex(I),Value)</code></td><td><code>player_pos_x(agent,objectIndex(I),Value)</code></td></tr></tbody></table> <p>ThinkEngine will then use <code>player_pos_x</code> in all generated fact files and reasoning cycles instead of the default.</p> <h3 id="why-this-matters">Why this matters</h3> <p>1. Makes your ASP code cleaner and more semantic (e.g., <code>player_pos_x</code> instead of <code>agentSensor_x</code>).</p> <p>2. Avoids naming conflicts when multiple agents or prefabs share similar property mappings.</p> <p>3. Helps maintain consistency between your <code>.asp</code> files and sensor configuration.</p> <p>Renaming predicates does <strong>not</strong> affect the runtime behavior of the sensor — it only changes the name of the fact passed to the solver.</p> <hr/> <h2 id="32-actuatorconfiguration">3.2 ActuatorConfiguration</h2> <p>An <strong>ActuatorConfiguration</strong> defines which GameObject properties are controlled by the reasoner.</p> <p>At runtime, when the solver produces an answer set, the mapped properties are updated accordingly.</p> <h3 id="adding-an-actuator">Adding an Actuator</h3> <p>1. Select the GameObject you want to control (e.g., <code>Agent</code>).</p> <p>2. Click <strong>Add Component → Actuator Configuration</strong>.</p> <p>3. Give the configuration a unique name (e.g., <code>agentActuator</code>).</p> <p>4. Select the properties to control (e.g., <code>DesiredX</code>, <code>DesiredY</code>, <code>DesiredZ</code> from your <code>AgentMover</code> script).</p> <p>5. Optionally, choose a <strong>trigger</strong> to decide when the actuator applies updates (e.g., <code>Always</code> or a custom trigger from <code>ThinkEngineTrigger.cs</code>).</p> <p>⚡ <strong>Note:</strong> When you save the configuration, ThinkEngine automatically adds a <code>MonoBehaviourActuatorsManager</code> to the same GameObject.</p> <hr/> <h2 id="33-prefab-support">3.3 Prefab Support</h2> <p>You can attach <strong>SensorConfiguration</strong> and <strong>ActuatorConfiguration</strong> directly to Prefabs as well as scene instances.</p> <p>At runtime, ThinkEngine will instantiate and wire them exactly like on scene objects.</p> <p><strong>Project Convention:</strong> place Prefabs under <code>Assets/Prefab</code>.</p> <ul><li><p>Keep configuration names unique per prefab type to avoid ambiguous mapping at runtime.</p></li> <li><p>When debugging, remember that each prefab instance will generate its own <code>objectIndex(...)</code> in facts.</p></li></ul> <hr/> <h2 id="34-linking-properties-to-asp-facts">3.4 Linking Properties to ASP Facts</h2> <p>Once sensors and actuators are configured, ThinkEngine automatically generates their ASP mapping.</p> <p><strong>Example:</strong></p> <p>Given:</p> <ul><li><p>Sensor name: <code>agentSensor</code></p></li> <li><p>Actuator name: <code>agentActuator</code></p></li> <li><p>Property: <code>transform.position.x</code></p></li></ul> <p>The corresponding facts might look like:</p> <pre class="language-prolog"><!></pre> <p>These are the facts and atoms that the solver receives and emits.</p> <hr/> <h2 id="35-multiple-sensors-and-actuators">3.5 Multiple Sensors and Actuators</h2> <p>You can add multiple <strong>SensorConfiguration</strong> and <strong>ActuatorConfiguration</strong> components in the same scene:</p> <ul><li><p>A single Brain can consume data from several sensors.</p></li> <li><p>A single GameObject can have multiple sensors and actuators, if needed.</p></li> <li><p>Multiple GameObjects can each expose their own sensors and actuators to the same Brain.</p></li></ul> <p><strong>Best practice:</strong> Keep sensor and actuator names unique across the project to avoid index collisions and mapping ambiguity.</p> <hr/> <h2 id="36-triggers">3.6 Triggers</h2> <p>Triggers determine <strong>when</strong> sensors are read and actuators are applied.</p> <p>ThinkEngine provides a default trigger:</p> <ul><li><p>“When sensors are ready” for brains</p></li> <li><p>“Always” for actuators</p></li></ul> <p>You can define custom triggers by editing <code>ThinkEngineTrigger.cs</code>:</p> <pre class="language-csharp"><!></pre> <p>These functions then appear in the trigger dropdown menu of <strong>Brain</strong> and <strong>Actuator</strong> components.</p> <hr/> <h2 id="37-runtime-behavior">3.7 Runtime Behavior</h2> <p>When the game starts, <code>MonoBehaviourSensorsManager</code> begins collecting data.</p> <p>Each reasoning cycle:</p> <p>1. Sensor values are translated into ASP facts.</p> <p>2. The solver is called by the Brain.</p> <p>3. If the solver returns actions, Actuators apply the new values to the GameObject properties.</p> <hr/> <h2 id="38-debugging-sensor-and-actuator-mapping">3.8 Debugging Sensor and Actuator Mapping</h2> <ul><li><p>You can preview generated sensor and actuator scripts in the Inspector after saving configurations.</p></li> <li><p>If actuators aren’t applying values:</p></li></ul> <p>  - Check that the actuator property names exactly match what’s used in the <code>.asp</code> file.</p> <p>  - Ensure your Trigger is firing.</p> <p>  - Verify the solver is producing matching <code>setOnActuator(...)</code> atoms.</p> <p>🧰 <strong>Tip:</strong> Maintaining clean naming conventions like <code>agentSensor</code> and <code>agentActuator</code> avoids most mapping issues.</p> <p><img src="/images/sensor-actuator.png" alt="Sensor &amp; Actuator Configuration"/></p>',1);function C(i){var r=k(),e=a(g(r),46),c=t(e);s(c,()=>`<code class="language-prolog"><span class="token function">agentSensor_x</span><span class="token punctuation">(</span>agent<span class="token punctuation">,</span> <span class="token function">objectIndex</span><span class="token punctuation">(</span><span class="token number">43918</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">"3.14"</span><span class="token punctuation">)</span>
</code>`),o(e);var n=a(e,80),d=t(n);s(d,()=>`<code class="language-prolog"><span class="token function">agentSensor_x</span><span class="token punctuation">(</span>agent<span class="token punctuation">,</span> <span class="token function">objectIndex</span><span class="token punctuation">(</span><span class="token number">43918</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token string">"0.0"</span><span class="token punctuation">)</span><span class="token operator">.</span>

<span class="token function">setOnActuator</span><span class="token punctuation">(</span><span class="token function">agentActuator_desiredX</span><span class="token punctuation">(</span>agent<span class="token punctuation">,</span> <span class="token function">objectIndex</span><span class="token punctuation">(</span><span class="token number">43918</span><span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token number">1.0</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token operator">.</span>
</code>`),o(n);var p=a(n,26),l=t(p);s(l,()=>`<code class="language-csharp">
<span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> <span class="token function">AlwaysTrue</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token boolean">true</span><span class="token punctuation">;</span>

<span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> <span class="token function">Every10Frames</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=></span> Time<span class="token punctuation">.</span>frameCount <span class="token operator">%</span> <span class="token number">10</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">;</span>
</code>`),o(p),m(32),h(i,r)}export{C as default,f as metadata};
