using System;
using System.Collections.Generic;
using UnityEngine;

namespace ThinkEngine
{
    class CLASS_NAME : Sensor
    {
FIELDS

        //Singleton
        protected static Sensor instance = null;

        internal static Sensor Instance
        {
            get
            {
                if (instance == null)
                {
					instance = new CLASS_NAME();
                }
                return instance;
            }
        }

		internal override void Initialize(GameObject gameObject)
		{
			// Method called on initializing sensors
            //Debug.Log("Initialize method called!");
			this.gameObject = gameObject;
INITIALIZATION
		}

		internal override void Destroy()
		{
			// Method called on destroing sensors
            //Debug.Log("Destroy method called!");
			instance = null;
		}

		internal override void ManageSensor()
		{
            // Method called on manageing sensors
            //Debug.Log("ManageSensor method called!");
MANAGESENSOR
		}
    }
}